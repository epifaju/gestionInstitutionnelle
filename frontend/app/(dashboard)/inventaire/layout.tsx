import { DashboardShell } from "@/components/dashboard-shell";

export default function InventaireSectionLayout({ children }: { children: React.ReactNode }) {
  return <DashboardShell>{children}</DashboardShell>;
}
